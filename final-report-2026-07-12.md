# 최종 보고서 — 두 블로그 가독성·디자인·운영 체계 개선 (2026-07-12)

대상: 코너 속의 코너(goodlookingprokim.github.io) · 4060미들스쿨(goodlookingprokim.github.io/4060-middle-school)

## 한 줄 요약

갤러리 가독성 문제의 원인(좁은 고정 높이 iframe)을 제거하고, 4060을 중장년 독자 기준으로 조정했으며, 한글 줄바꿈·라벨 폰트 문제를 두 블로그 공통으로 해결했습니다. 무엇보다 이 모든 판단을 에이전트가 매번 참고하는 지침 문서 체계로 고정해, 같은 문제가 재발하지 않고 새 문제는 사례집에 쌓이도록 만들었습니다.

## 1. 해결한 문제와 결과

**갤러리 가독성 (4060).** 발표자 HTML을 441px 본문 컬럼의 고정 높이 iframe에 밀어 넣던 방식이 글자 겹침·표 잘림·이중 스크롤의 원인이었습니다. 5건 모두 판정 후 A형(전체 화면 카드, 2건) 또는 B형(1280px 렌더링 후 축소 미리보기, 3건)으로 교체했습니다. 강사 원문은 단 1바이트도 수정하지 않았고, 강사에게는 어떤 안내도 하지 않는 것을 원칙으로 지침에 못 박았습니다. 입문 매뉴얼북의 미리보기 PNG는 4060 에이전트가 이어받아 완료했습니다.

**한글 줄바꿈 (공통).** 홈 카드에서 "모임인지"가 "모/임인지"로 잘리던 문제는 body에 `word-break: keep-all`을 적용해 해결했고, 배포 화면에서 단어 단위 줄바꿈을 확인했습니다.

**한글 라벨 자간 (공통).** 카드 하단 라벨("원문 보관 · 웹 보기")이 벌어져 보이던 원인은 라벨이 한글 글리프가 없는 JetBrains Mono를 쓰는 것이었습니다. 1차 시도(codeFont 뒤에 한글 폰트 덧붙이기)는 codeFont 스택 중간의 제네릭 monospace가 모든 글자를 가로채 무효임을 배포 화면에서 확인했고, 최종적으로 한글 라벨 5종은 본문 폰트로 전환, 영문 라벨(킥커·태그)만 모노를 유지했습니다. 이 교훈은 사례집에 기록했습니다.

**4060 독자 맞춤 (4060만).** 본문 18px(모바일 17px), 링크 밑줄 강화, 코너에서 검증된 표 처리 규칙 이식, 홈 목록 24건 → 6건 축소, 리더 모드 안내 추가, 탐색기에서 갤러리 폴더 상단 정렬.

## 2. 재발 방지 체계 (이번 작업의 핵심 자산)

| 문서 | 역할 | 위치 |
|------|------|------|
| AGENT_OPERATIONS_GUIDE.md (v1.1) | 발행 파이프라인 5단계, 발행 전 3장 스크린샷 검증, 금지 사항, **부록: 트러블슈팅 사례집** | 두 저장소 루트 (동일 내용) |
| GALLERY_AGENT_GUIDE.md (v1.2) | 갤러리 접수 절차 6단계, A/B형 템플릿, 판정 기준, 강사 응대 원칙(지침 전달 금지) | 4060 저장소 루트 |
| DESIGN_TOKENS.md (v1.2) | 공통 색·폰트·레이아웃 + 블로그별 의도된 차이 표. 표에 없는 차이 = 드리프트 | 두 저장소 루트 (동일 내용) |
| OPENCLAW_BLOG_TEMPLATE.md | 글 작성 형식. 상단에 지침 우선순위, 임베드 규칙 신설 | 두 저장소 (기존 문서 개선) |

작동 방식: 두 에이전트의 시스템 지침(AGENTS.md)에 "블로그 작업 전 AGENT_OPERATIONS_GUIDE.md 필독"이 들어가 있으므로, 사람이 기억하지 않아도 규칙이 적용됩니다. 새 문제를 해결하면 사례집 표에 한 줄만 추가하면 됩니다 — 오늘 확정한 6개 사례(한글 줄바꿈, 라벨 폰트, iframe 임베드, 플랫폼 빌드 오류, git 잠금, 배포 지연)가 이미 들어 있습니다.

## 3. 최종 검증 결과

두 저장소 모두 원격과 동기화된 상태에서 배포 화면을 직접 확인했습니다: 4060 홈의 단어 단위 줄바꿈 정상, 홈 목록 6건 + 허브 링크 정상, 갤러리 미리보기 정상. 라벨 폰트 최종 수정안은 배포 전 실제 페이지에 스타일을 주입해 시각 검증을 마친 뒤 CSS에 반영했습니다. 남은 발행 대기분은 4장 참조.

## 4. 이 보고서 시점의 미발행 변경분

한글 라벨 폰트 최종 수정(custom.scss)과 문서 업데이트(운영 지침 v1.1, 토큰 v1.2, 이 보고서)가 두 저장소에 커밋되어 푸시 대기 중입니다. 각 저장소에서 `git push`만 실행하면 완료됩니다(잠금 파일 오류가 나면 사례집의 해결법 참조).

## 관련 문서

상세 진단: `4060-gallery-readability-review-2026-07-12.md` · 디자인 검토: `design-usability-improvement-plan-2026-07-12.md`
