# AGENT OPERATIONS GUIDE — 블로그 운영 에이전트 공통 지침

적용 대상: 코너 속의 코너(goodlookingprokim.github.io), 4060미들스쿨(goodlookingprokim.github.io/4060-middle-school)
버전: 1.1 (2026-07-12) · 이 문서는 두 저장소에 동일한 내용으로 존재해야 한다. 수정 시 양쪽에 함께 반영한다.

## 0. 문서 우선순위

에이전트는 작업 전 반드시 아래 순서로 지침을 확인한다. 상위 문서가 하위 문서와 충돌하면 상위 문서를 따른다.

1. `AGENT_OPERATIONS_GUIDE.md` (이 문서) — 운영·검증·품질 기준
2. 블로그별 전용 지침 — 4060은 `GALLERY_AGENT_GUIDE.md` (갤러리·외부 자료 처리)
3. `OPENCLAW_BLOG_TEMPLATE.md` — 글 작성 형식·강조 블록·허브 규칙

## 1. 운영 원칙

1. 모든 발행물은 "게시 후 확인"이 아니라 "확인 후 게시"한다. 발행 전 검증(3장)을 건너뛰지 않는다.
2. 타인이 만든 파일(발표자료, 공유 문서)은 바이트 단위로 무수정 보존한다. 가독성 문제는 파일이 아니라 보여주는 방식(임베드·레이아웃)으로 해결한다.
3. 독자가 다르면 기본값도 다르다. 코너 속의 코너는 실무 독자 기준, 4060은 중장년 독자 기준(더 큰 글자, 더 높은 대비, 더 단순한 동선)을 우선한다.
4. 테마 색·타이포그래피 변경은 반드시 `DESIGN_TOKENS.md`(5장)를 먼저 수정한 뒤 두 저장소의 `quartz.config.ts`에 반영한다. 한쪽만 바꾸지 않는다.

## 2. 발행 파이프라인 (모든 글 공통)

각 단계를 순서대로 수행하고, 실패 시 다음 단계로 넘어가지 않는다.

1. 작성 — `content/templates/`의 템플릿과 `OPENCLAW_BLOG_TEMPLATE.md` 규칙을 따른다.
2. 로컬 빌드 — `npx quartz build`가 오류 없이 끝나는지 확인한다.
3. 발행 전 검증 — 3장의 체크리스트를 수행한다.
4. 커밋·푸시 — 커밋 메시지에 `[verified]` 태그를 붙여 검증을 마쳤음을 기록한다.
5. 배포 확인 — 배포된 실제 URL을 한 번 열어 페이지가 존재하고 제목·설명이 맞는지 확인한다.

## 3. 발행 전 검증 체크리스트 (필수)

새 페이지 또는 수정된 페이지에 대해 headless 브라우저로 스크린샷 3장을 찍는다: 라이트 모드 1280px, 다크 모드 1280px, 모바일 390px. 각 스크린샷에서 아래 5가지를 확인한다.

| 항목 | 통과 기준 |
|------|-----------|
| 글자 겹침 | 텍스트가 다른 텍스트·이미지와 겹쳐 보이지 않는다 |
| 대비 | 본문 글자와 배경의 명도 대비가 뚜렷하다 (WCAG 2.1 기준 4.5:1 이상 목표) |
| 표·코드 잘림 | 표와 코드 블록이 가로로 잘리지 않거나, 잘리는 경우 가로 스크롤이 동작한다 |
| 이미지 | 깨진 이미지(alt 텍스트만 표시)가 없다 |
| 이중 스크롤 | 페이지 안에 독립적으로 스크롤되는 고정 높이 임베드가 없다 |
| 링크 목적지 | 본문 링크를 클릭했을 때 현재 블로그 밖(루트 도메인·다른 블로그)으로 이동하지 않는다. 예시 주소는 코드 표기, 불가피한 외부 참조는 새 창 + 외부 표시 |

하나라도 실패하면 게시하지 않고 원인을 수정한 뒤 재검증한다. 외부 자료(갤러리) 페이지에서 실패 원인이 원문 자체에 있으면 원문을 고치지 말고 블로그별 전용 지침의 예외 처리 규칙을 따른다.

## 4. 금지 사항

1. 고정 높이 iframe 임베드(`height: NNvh` 또는 고정 px)를 본문에 넣지 않는다. 미리보기가 필요하면 축소(scale) 미리보기 또는 첫 화면 PNG 카드를 사용한다.
2. 존재하지 않는 글로 가는 링크(가짜 링크)를 만들지 않는다.
3. 검증 없이 테마 색상 값을 임의로 바꾸지 않는다.
4. 타인 제공 파일을 수정·재저장하지 않는다(줄바꿈·인코딩 변환 포함).
5. `content/` 밖의 운영 문서(이 지침 포함)를 발행 대상으로 옮기지 않는다.

## 5. 디자인 토큰 관리

두 블로그의 공통 기준값은 `DESIGN_TOKENS.md`에 기록한다. 이 문서가 아직 없으면 현재 각 저장소 `quartz.config.ts`의 theme 값을 그대로 옮겨 만든 뒤 관리한다. 최소 포함 항목: 라이트/다크 모드 색상 팔레트, 제목·본문·코드 폰트와 크기 기준, 블로그별 차이(4060의 본문 글자 크기 상향 등)와 그 이유.

## 6. 정기 점검 (월 1회 권장)

1. 두 저장소의 이 문서와 `DESIGN_TOKENS.md`가 동일한지 비교한다.
2. 갤러리·외부 자료 페이지를 라이트/다크 모드로 열어 3장 체크리스트를 다시 통과하는지 확인한다.
3. 깨진 외부 링크가 있는지 확인한다.
4. 상단 메뉴(quartz.layout.ts의 masthead links)가 실제 콘텐츠 분류와 일치하는지 확인한다. 새 카테고리·갤러리가 생기면 메뉴에도 반영한다.
5. 글 속의 "예시 주소"는 링크가 아니라 인라인 코드(백틱)로 표기됐는지 확인한다. 예시 주소가 링크로 렌더링되면 404 링크가 된다.

## 부록. 트러블슈팅 사례집 (같은 문제를 두 번 조사하지 않기 위한 기록)

| 증상 | 원인 | 해결 | 확정일 |
|------|------|------|--------|
| 한글이 "모/임인지"처럼 음절 단위로 잘림 | 브라우저 기본 줄바꿈은 한글을 아무 글자에서나 자름 | body에 `word-break: keep-all` + `overflow-wrap: break-word` (긴 URL 넘침 보완). 표 셀은 `break-word` 예외 유지 | 2026-07-12 |
| 한글 라벨(칩·메타·브레드크럼)의 자간이 벌어져 보임 | 라벨이 codeFont(JetBrains Mono) 사용 → 한글 글리프가 없어 시스템 모노 폴백 + 넓은 모노 공백 | 한글 라벨은 `var(--bodyFont)`로 전환. **주의: `var(--codeFont), var(--bodyFont)`처럼 뒤에 덧붙이는 방식은 무효** — codeFont 스택 중간의 제네릭 `monospace`가 모든 글자를 가로챔. 영문 전용 라벨(킥커·태그)만 codeFont 유지 | 2026-07-12 |
| 외부 HTML 자료가 본문에서 겹치고 잘림 | 데스크톱 폭 설계 자료를 좁은 본문 컬럼의 고정 높이 iframe에 임베드 | 고정 높이 iframe 금지. GALLERY_AGENT_GUIDE의 A형(전체 화면 카드)/B형(scale 축소 미리보기) 사용 | 2026-07-12 |
| 다른 플랫폼(샌드박스 등)에서 `quartz build` 실패 (esbuild platform 오류) | node_modules가 설치된 OS 전용 바이너리 포함 | 빌드·검증은 저장소를 npm install한 그 컴퓨터에서 실행. 다른 환경에서는 구문 검증(중괄호·frontmatter)으로 대체 | 2026-07-12 |
| git 명령이 "index.lock exists" 오류 | 샌드박스 등 권한 제한 환경에서 git 작업 후 잠금 파일이 안 지워짐 | 저장소에서 `rm -f .git/*.lock .git/objects/maintenance.lock` 후 재시도 | 2026-07-12 |
| 글 안의 링크가 404 (존재하지 않는 페이지) | 강의 자료 속 "예시 주소"가 자동 링크로 렌더링됨 (예: 새 글 주소 예시) | 예시 주소는 백틱 인라인 코드로 표기해 클릭 불가로 만들고 "형식 예시" 표시. 실제 대상이 있어야 할 링크면 대상 슬러그 확인 | 2026-07-12 |
| 4060 글을 읽다가 클릭하면 코너 속의 코너(루트 도메인)로 이동 | 실습 교육 글의 예시 주소(https://goodlookingprokim.github.io)가 클릭 가능한 링크로 렌더링됨. 코드 블록 안 주소는 안전하지만 본문의 맨 URL과 마크다운 링크는 클릭됨 | 예시 주소는 백틱 코드 + "예시" 표시, 실제 외부 참조는 `<a target="_blank">` + "(블로그 밖 · 새 창)" 표시. 발행 전 점검: `grep -rn "goodlookingprokim.github.io" content/ --include="*.md" \| grep -v 4060-middle-school` 결과가 코드 표기·새 창 처리 외 0건이어야 함 | 2026-07-12 |
| 4060 허브/읽기 경로 카드를 클릭하면 코너(루트 도메인)의 404로 이동 | Quartz가 원시 HTML(`<a href="./글이름">`) 상대 링크를 변환할 때 서브 경로 배포(baseUrl의 /4060-middle-school)를 반영하지 않고 루트 절대경로(`/topics/...`)로 출력함. 마크다운 링크는 정상 변환되지만 hub-card, reading-path, action-row 같은 원시 HTML 블록의 href가 당함. 루트 배포인 코너에서는 증상이 안 보임 | **원시 HTML 블록의 href는 항상 전체 URL**(https://...github.io/4060-middle-school/...)로 쓴다. 상대경로(./, ../)와 베이스 없는 절대경로(/) 금지. 2026-07-12에 두 블로그 13개 인덱스 파일 전량 전환 완료 | 2026-07-12 |
| 탐색기(Explorer)가 갑자기 비어 보임 (데스크톱·모바일 공통) | Explorer의 커스텀 sortFn/filterFn/mapFn은 문자열로 직렬화되어 브라우저에서 실행되는데, slugSegment가 없는 노드(루트 등)에서 `.endsWith` 호출 → TypeError → 탐색기 렌더링 전체 중단. 빌드·tsc는 통과하므로 배포 후에만 드러남 | Explorer 옵션 함수 안에서는 모든 속성 접근을 방어한다: `(n.slugSegment ?? "")`. 수정 후 반드시 배포된 페이지에서 탐색기 항목 수를 확인한다. 브라우저 콘솔 오류 확인도 병행 | 2026-07-12 |
| 푸시했는데 사이트에 반영 안 보임 | GitHub Pages 배포에 2~3분 소요 + 브라우저 캐시 | 몇 분 기다린 뒤 강력 새로고침(Cmd+Shift+R) | 2026-07-12 |

새 문제를 해결하면 이 표에 한 줄 추가한다. 두 저장소의 이 문서를 함께 갱신한다.

## 변경 이력

- 1.1 (2026-07-12): 트러블슈팅 사례집 부록 추가.
- 1.0 (2026-07-12): 최초 작성. 4060 갤러리 가독성 진단(4060-gallery-readability-review-2026-07-12.md)의 후속 조치.
