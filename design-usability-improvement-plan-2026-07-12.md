# 두 블로그 디자인·편의성·가독성 개선안

작성일: 2026-07-12 · 대상: 코너 속의 코너, 4060미들스쿨 · 전제: 에이전트 2개 분리 운영 유지, 운영 부담 증가 없음

## 먼저 결론

두 블로그는 같은 테마(색·폰트·레이아웃)를 공유하는 건강한 구조이고, 디자인을 갈아엎을 이유가 없습니다. 필요한 것은 세 가지였습니다. 첫째, 독자층이 다른데 기본값이 같았던 것을 바로잡기 — 4060만 본문 글자를 키우고 링크를 더 뚜렷하게. 둘째, 한쪽에만 있던 검증된 개선(표 처리 규칙)을 다른 쪽에 이식하기. 셋째, 이 차이와 공통 기준을 문서(`DESIGN_TOKENS.md`)로 고정해 앞으로 두 에이전트가 서로 어긋나지 않게 하기. 이 세 가지는 모두 적용을 마쳤고, 판단이 필요한 제안 4가지는 아래에 남겼습니다.

## 1. 현황 진단 (실측 기준)

양쪽 `quartz.config.ts`와 `custom.scss`(약 1,100줄)를 비교한 결과, 색상 팔레트·폰트·레이아웃 변수는 완전히 동일했고 차이는 두 가지뿐이었습니다. 코너 속의 코너에만 있던 표 처리 규칙(`table-layout: fixed`, 긴 링크 줄바꿈 — 표가 컬럼을 뚫고 나가는 것을 막는 규칙)과, 4060에만 있는 갤러리 스타일입니다. 즉 두 블로그는 사실상 하나의 디자인 시스템을 쓰고 있었지만, 그 사실이 어디에도 기록되어 있지 않아 언제든 어긋날 수 있는 상태였습니다.

구조 면에서는 양쪽 모두 강점이 많습니다. 허브형 카테고리 인덱스, 다크모드, 검색, 탐색기, 목차, 백링크가 이미 갖춰져 있고, 본문 폭 780px과 줄 간격 1.78은 한글 장문 독서에 적절한 값입니다.

문제는 기본값이 한 종류라는 점이었습니다. 본문 16.5px은 실무 독자(코너)에게는 적당하지만, 4060의 중장년 독자에게는 작습니다. 링크 밑줄도 45% 농도의 옅은 선이라 구분이 약했습니다.

## 2. 이번에 적용한 것 (4060미들스쿨)

| 항목 | 변경 | 효과 |
|------|------|------|
| 본문 글자 크기 | 16.5px → 18px (모바일 16px → 17px) | 중장년 독자 체감 효과가 가장 큰 단일 변경 |
| 링크 밑줄 | 농도 45% → 70% | 링크가 본문과 뚜렷이 구분됨 |
| 표 처리 규칙 | 코너 속의 코너의 검증된 규칙 이식 | 긴 링크·긴 셀이 표를 밀어내는 현상 방지 |
| 갤러리 표시 방식 | (앞서 완료) 고정 높이 iframe 제거, A형/B형 체계 | 갤러리 가독성 문제 해소 |

코너 속의 코너는 변경하지 않았습니다. 실무 독자 기준으로 현행 값이 적정하고, "독자가 다르면 기본값도 다르다"가 이번 개선의 원칙이기 때문입니다.

글자 크기 변경이 왜 안전한가를 설명하면: 두 블로그의 모든 크기는 `--reading-size` 변수 하나에서 파생되도록 만들어져 있습니다(rem 단위). 변수 하나를 바꾸면 제목·본문·카드가 비례해서 함께 커지므로, 레이아웃이 깨질 위험이 매우 낮습니다. 이런 구조 덕분에 CSS 한 줄 수정으로 끝났습니다.

## 3. 운영이 어긋나지 않게 하는 장치

`DESIGN_TOKENS.md`를 작성해 두 저장소 루트에 동일하게 배치했습니다. 공통 토큰(색·폰트·레이아웃)과 "의도된 차이" 표(4060의 18px 등)를 기록했고, 표에 없는 차이가 발견되면 드리프트로 간주해 문서 기준으로 맞추도록 했습니다. `AGENT_OPERATIONS_GUIDE.md`는 이미 "테마 변경은 이 문서를 먼저 고치고 양쪽에 반영"을 규칙으로 갖고 있으므로, 두 에이전트는 앞으로 이 문서를 통해 동기화됩니다. 월 1회 점검 시 두 저장소의 `custom.scss`를 diff해 의도되지 않은 차이를 잡아내면 됩니다.

## 4. 제안 — 판단이 필요해 적용하지 않은 것

**첫째, 4060 홈의 글 목록 다이어트.** 현재 홈의 "지금 먼저 보면 좋은 글"에 20건 이상이 한 번에 나열됩니다. 중장년 독자에게 긴 목록은 선택 부담입니다. 최근 5~7건만 보여주고 나머지는 "이번 주제" 허브로 보내는 편이 낫습니다. 에이전트가 홈 index.md를 관리하므로, 지침에 "홈 목록은 최근 N건까지"를 추가하면 자동 유지됩니다.

**둘째, 4060 탐색기 기본 펼침.** 갤러리 세 개가 접힌 상태로 시작해 발견성이 낮습니다. 갤러리 폴더만 기본 펼침으로 두면 발표자 자료가 더 잘 보입니다(quartz.layout.ts의 Explorer 설정).

**셋째, 글자 크기 조절 버튼.** 18px 상향으로 대부분 해결되지만, 독자가 직접 한 단계 더 키울 수 있는 버튼(가/가+)을 헤더에 넣는 방법도 있습니다. Quartz 기본 컴포넌트가 아니라 커스텀 컴포넌트 작업이 필요하므로, 18px의 반응을 본 뒤 결정해도 늦지 않습니다.

**넷째, 모임용 "크게 보기" 모드.** 모임 자리에서 화면을 함께 볼 때를 위해 리더 모드(이미 있음) 안내를 홈에 한 줄 넣는 것. 작업은 거의 없고 4060 모임 특성에 맞습니다.

## 5. 반영 및 확인 방법

적용된 변경은 4060 저장소의 `quartz/styles/custom.scss` 한 파일이며 커밋 전 상태입니다. 로컬에서 `npx quartz build` 후 아무 글이나 열어 글자 크기와 표를 확인하고 푸시하면 됩니다. SCSS 중괄호 균형(161/161)은 검증을 마쳤습니다. 제안 4가지 중 진행할 항목을 알려주시면 이어서 작업합니다.
